const trueFalse = (answer) => {
  return (answer === 'I am in the sixth grade'
    ? 'You are in the same grade as Me!'
    : 'You are in a different grade');
};

console.log(trueFalse('Test'));
console.log(trueFalse('I am in the sixth grade'));
